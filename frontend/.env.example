# ⚠️ EXPO_PUBLIC_* 變數會在 Metro build 時就烤進 JS bundle，
#    必須在「build APK / expo start」之前就設好；build 之後才改 .env 不會生效。

# 後端 API 位址。實機測試不能用 localhost（那是手機自己），須用開發機可連的位址。
#   LAN:       http://192.168.x.x:8000
#   Tailscale: http://100.x.y.z:8000  或  http://<host>.<tailnet>.ts.net:8000
#   Emulator:  http://10.0.2.2:8000
#   詳見 docs/deployment.md
EXPO_PUBLIC_API_BASE_URL=http://192.168.x.x:8000

# Google Maps Android SDK key（Android 地圖用；build 時注入 AndroidManifest）
# iOS 用 Apple Maps 免此 key；Expo Go 內用 Expo 內建地圖、不會用到你的 key（你的 key 只在 dev build / APK 生效）。
# 與後端的 GOOGLE_ROUTES_API_KEY 應為【不同的兩把 key】，各自套用不同限制。
GOOGLE_MAPS_ANDROID_KEY=AIzaSy...

# 選用（dev-only，Tailscale 開發）：讓 Metro 對外宣告 tailnet IP，手機才連得到 Metro 抓 JS bundle。
# 只在用 Tailscale 開發時取消下一行註解並填自己的 tailnet IP；打包 APK 後不需要。
# Expo CLI 會把 .env 載入 process 環境，Metro 讀此變數；驗證方式見 docs/deployment.md §2.5。
# REACT_NATIVE_PACKAGER_HOSTNAME=100.x.y.z
